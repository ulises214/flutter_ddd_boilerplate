part of '../domain.dart';

/// The username for the user
class UserUsername extends RequiredStringValueObject {
  /// Initialize the username for the user
  UserUsername(String value) : super(value);
}
